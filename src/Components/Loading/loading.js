import React from 'react';
import { LoadingContainer, Loading } from './loadingStyles';

const loading = () => {
  return (
    <Loading>
      <LoadingContainer />
    </Loading>
  );
};

export default loading;
